[project]
name = "spectral-model"
version = "0.1.0"
description = "Training data pipeline for Spectral-IAM model"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
spectral = "cli:app"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
